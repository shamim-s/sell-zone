import React from 'react';

const Spinner = () => {
    return (
        <span className='w-5 h-5 border-2 border-dashed border-white
         rounded-full animate-spin flex justify-center items-center'>
        </span>
    );
};

export default Spinner;