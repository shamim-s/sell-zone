import React from 'react';

const Loader = () => {
    return (
        <span className='w-6 h-6 border-2 border-dashed border-primary
        rounded-full animate-spin flex justify-center items-center'>

        </span>
    );
};

export default Loader;